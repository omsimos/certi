export type Event = {
  title: string;
  organizer: string;
  description: string;
  date: string;
};

export type Attendee = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
};
